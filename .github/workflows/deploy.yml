name: Deploy to Production

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: 107.173.35.23
          username: ploi
          key: ${{ secrets.DEPLOY_SSH_KEY }}
          script: |
            cd /home/ploi/foodrecord.webprvw.xyz
            git pull origin main
